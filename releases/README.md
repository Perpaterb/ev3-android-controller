# Releases

Signed `.aab` builds land here, named `bricklogic-<version>-<build>.aab`.
The binaries themselves are git-ignored — produce them with
`./scripts/build_release.sh` (see [BUILD.md](../BUILD.md)).
